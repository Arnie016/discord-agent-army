{
  "name": "discord-agent-army-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "find scripts tests -name '*.mjs' -print0 | xargs -0 -n1 node --check"
  }
}
